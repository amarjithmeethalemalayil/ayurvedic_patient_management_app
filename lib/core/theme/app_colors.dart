import 'package:flutter/material.dart';

class AppColors {
  static const primaryTextColor = Colors.black;
  static const buttonColor = Color(0xFF006837);
  static const buttonTextColor = Colors.white;
  static const normalTextFieldColor = Color(0x40D9D9D9);
  static const termsAndConditionTextColor = Color(0xFF0028FC);
}